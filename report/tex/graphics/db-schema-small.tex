\documentclass[a3paper, landscape]{article}
\usepackage[left=0cm, right=0cm, top=0cm, bottom=0cm]{geometry}
\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[russian]{babel}
\thispagestyle{empty}

\usepackage{tabularray}
\usepackage{tikz}
\usetikzlibrary{positioning, calc, tikzmark}

\newcommand{\tm}[2]{\tikzmarknode{#1}{#2}}

\tikzset{
    lnk/.style={thick},
    crd/.style={font=\scriptsize, fill=white, inner sep=1pt},
    ann/.style={font=\scriptsize},
}

\newcommand{\dbtable}[3]{%
    \node[inner sep=0pt, anchor=north] (#1) at (#2) {%
        \begin{tblr}{
            colspec = {Q[c,1.5em]Q[l,12em]},
            hline{1-3} = {solid},
            hline{Z} = {solid},
            vlines,
            rows = {font=\ttfamily\footnotesize, abovesep=2pt, belowsep=2pt},
            row{1} = {font=\bfseries\small, halign=c},
            column{1} = {font=\it\footnotesize, halign=c},
        }
            #3
        \end{tblr}%
    };%
}

\newcommand{\dbdict}[3]{%
    \node[inner sep=0pt, anchor=north] (#1) at (#2) {%
        \begin{tblr}{
            colspec = {Q[c,1.5em]Q[l,7em]},
            hline{1-3} = {solid},
            hline{Z} = {solid},
            vlines,
            rows = {font=\ttfamily\footnotesize, abovesep=2pt, belowsep=2pt},
            row{1} = {font=\bfseries\scriptsize, halign=c},
            column{1} = {font=\it\footnotesize, halign=c},
        }
            #3
        \end{tblr}%
    };%
}

\begin{document}
\begin{figure}[p]
\centering
\scalebox{1.15}{
\begin{tikzpicture}[remember picture]

% === Shifts ===
\def\hgap{1.5em}
\def\xWestShift{-3.4em}
\def\xEastShift{12.6em}
\def\dxWestShift{-3.4em}
\def\dxEastShift{7.7em}
\def\oneRowTablesLinkMargin{7.9em}
\def\tableHgap{27em}

% ==================== ОСНОВНЫЕ ТАБЛИЦЫ ====================

% === Ряд 1 ===

\dbtable{ff}{\tableHgap,10em}{
    \SetCell[c=2]{c} Гарнитура & \\
    PK & \tm{ff_pk}{id\_Гарнитура} \\
       & Название \\
       & Год\_создания \\
       & Описание \\
       & Число\_начертаний \\
    FK & \tm{ff_fk_cat}{id\_Категория} \\
}

\dbtable{cat}{\tableHgap * 2,10em}{
    \SetCell[c=2]{c} Категория & \\
    PK & \tm{cat_pk}{id\_Категория} \\
       & Название \\
       & Описание \\
       & Иконка \\
       & Число\_гарнитур \\
}

% === Ряд 2 ===

\dbtable{tf}{\tableHgap,-18em}{
    \SetCell[c=2]{c} Начертание & \\
    PK & \tm{tf_pk}{id\_Начертание} \\
       & Название \\
    FK & \tm{tf_fk_w}{id\_Насыщенность} \\
    FK & \tm{tf_fk_sl}{id\_Наклон} \\
       & Размер\_файла \\
    FK & \tm{tf_fk_ff}{id\_Гарнитура} \\
    FK & \tm{tf_fk_fmt}{id\_Формат} \\
}

\dbtable{fmt}{\tableHgap * 2,-18em}{
    \SetCell[c=2]{c} Формат & \\
    PK & \tm{fmt_pk}{id\_Формат} \\
       & Название \\
    FK & \tm{fmt_fk_ext}{id\_Расширение} \\
       & Год\_появления \\
       & Описание \\
       & Поддержка\_веба \\
}

% === Ряд 3 ===

\dbtable{lang}{1.2em,-18em}{
    \SetCell[c=2]{c} Язык & \\
    PK & \tm{lang_pk}{id\_Язык} \\
       & Название \\
       & Код\_ISO \\
    FK & \tm{lang_fk_wd}{id\_Направление} \\
    FK & \tm{lang_fk_st}{id\_Тип\_письм.} \\
}

\dbtable{alph}{2em,10em}{
    \SetCell[c=2]{c} Символ & \\
    PK & \tm{alph_pk}{id\_Символ} \\
       & Значение \\
       & Код\_Unicode \\
    FK & \tm{alph_fk_tf}{id\_Начертание} \\
    FK & \tm{alph_fk_lang}{id\_Язык} \\
}

% ==================== СЛОВАРИ ====================

\dbdict{d_wd}{-18em,-12em}{
    \SetCell[c=2]{c} Напр.\_письма & \\
    PK & \tm{d_wd_pk}{id\_Направл.} \\
       & Название \\
}

\dbdict{d_st}{-18em,-20em}{
    \SetCell[c=2]{c} Тип\_письм. & \\
    PK & \tm{d_st_pk}{id\_Тип\_п.} \\
       & Название \\
}

% Верхний край
\dbdict{d_weight}{27em,-7em}{
    \SetCell[c=2]{c} Насыщенность & \\
    PK & \tm{d_weight_pk}{id\_Насыщ.} \\
       & Название \\
}

\dbdict{d_slope}{48em,-7em}{
    \SetCell[c=2]{c} Наклон & \\
    PK & \tm{d_slope_pk}{id\_Наклон} \\
       & Название \\
}

% Правый край
\dbdict{d_ext}{72em,-21.24em}{
    \SetCell[c=2]{c} Расширение & \\
    PK & \tm{d_ext_pk}{id\_Расшир.} \\
       & Название \\
}

% ==================== СВЯЗИ ОСНОВНЫХ ТАБЛИЦ ====================

% Гарнитура.id_Категория → Категория.id_Категория
\draw[lnk] ([xshift=\xEastShift]ff_fk_cat.west)
    -- ++(\hgap,0) node[crd, below] {$\infty$}
    node[ann, below, yshift=-0.5em] {15--20}
    |- ++(\oneRowTablesLinkMargin,0)
    |- ([xshift={-\hgap + \xWestShift}]cat_pk.west)
    -- node[crd, above] {1} ([xshift=\xWestShift]cat_pk.west);

% Гарнитура.id_Гарнитура → Начертание.id_Гарнитура
\draw[lnk] ([xshift=\xWestShift]ff_pk.west)
    -- ++(-2.2em,0) node[crd, above] {1}
    |- ++(-42em,6em)
    |- ([xshift={-\hgap + \xWestShift}]tf_fk_ff.west)
    -- node[crd, below, xshift=-1em] {$\infty$}
    node[ann, below, xshift=-1em, yshift=-0.5em] {18--22}
    ([xshift=\xWestShift]tf_fk_ff.west);

% Начертание.id_Формат → Формат.id_Формат
\draw[lnk] ([xshift=\xEastShift]tf_fk_fmt.west)
    -- ++(2.4em,0) node[crd, above] {$\infty$}
    |- ++(4.1em,0)
    |- ([xshift={-\hgap + \xWestShift}]fmt_pk.west)
    -- node[crd, above] {1} ([xshift=\xWestShift]fmt_pk.west);

% Символ.id_Начертание → Начертание.id_Начертание
\draw[lnk] ([xshift=\xEastShift]alph_fk_tf.west)
    -- ++(\hgap,0)
    -- ++(0, -3.35em)
    node[crd, above] {$\infty$}
    node[ann, below, fill=white] {2--5 языков на начертание}
    |- ([xshift={-\hgap + \xWestShift}]tf_pk.west)
    -- node[crd, above] {1} ([xshift=\xWestShift]tf_pk.west);

% Символ.id_Язык → Язык.id_Язык
\draw[lnk] ([xshift=\xWestShift]alph_fk_lang.west)
    -- ++(-\hgap,0)
    -- ++(0, -2em)
    node[crd, above] {$\infty$}
    node[ann, below, fill=white] {33--45 букв на пару}
    |- ([xshift=\xWestShift]lang_pk.west)
    -- node[crd, above] {1} ([xshift=\xWestShift]lang_pk.west);

% ==================== СВЯЗИ СЛОВАРЕЙ ====================

% Начертание.id_Насыщенность → Насыщенность
\draw[lnk] ([xshift=\xEastShift]tf_fk_w.west)
    -- ++(2.5em,0) node[crd, below] {$\infty$}
    |- ([xshift=\dxEastShift]d_weight_pk.west)
    -- node[crd, above] {1} ([xshift=\dxEastShift]d_weight_pk.west);

% Начертание.id_Наклон → Наклон
\draw[lnk] ([xshift=\xEastShift]tf_fk_sl.west)
    -- ++(\hgap*3,0) node[crd, below] {$\infty$}
    |- ([xshift={-\hgap + \dxWestShift}]d_slope_pk.west)
    -- node[crd, above] {1} ([xshift=\dxWestShift]d_slope_pk.west);

% Формат.id_Расширение → Расширение
\draw[lnk] ([xshift=\xEastShift]fmt_fk_ext.west)
    -- ++(\hgap,0) node[crd, above] {$\infty$}
    |- ([xshift={-\hgap + \dxWestShift}]d_ext_pk.west)
    -- node[crd, above] {1} ([xshift=\dxWestShift]d_ext_pk.west);

% Язык.id_Направление → Направление_письма
\draw[lnk] ([xshift=\xWestShift]lang_fk_wd.west)
    -- ++(-\hgap,0) node[crd, above] {$\infty$}
    -- ++(-0.6em, 0)
    |- ([xshift={\hgap + \dxEastShift}]d_wd_pk.west)
    -- node[crd, above] {1} ([xshift=\dxEastShift]d_wd_pk.west);

% Язык.id_Тип_письм. → Тип_письменности
\draw[lnk] ([xshift=\xWestShift]lang_fk_st.west)
    -- ++(-\hgap,0) node[crd, above] {$\infty$}
    -- ++(-2em, 0)
    |- ([xshift={\hgap + \dxEastShift}]d_st_pk.west)
    -- node[crd, above] {1} ([xshift=\dxEastShift]d_st_pk.west);


% ==================== УРОВНИ, ДИАПАЗОН КОЭФФИЦИЕНТА, КОЛИЧЕСТВО ЗАПИСЕЙ ====================

% Формат подписи:
% уровень; диапазон коэффициента; количество записей

% Базовая таблица
\node[ann, above=0.3em of cat] {1; --; 12};

% Словари
\node[ann, above=0.3em of d_wd] {1; --; 2};
\node[ann, above=0.3em of d_st] {1; --; 3};
\node[ann, above=0.3em of d_weight] {1; --; 9};
\node[ann, above=0.3em of d_slope] {1; --; 3};
\node[ann, above=0.3em of d_ext] {1; --; 5};

% Таблицы, зависящие от словарей
\node[ann, above=0.3em of fmt] {2; --; 5};
\node[ann, above=0.3em of lang] {2; --; 40};

% Производные таблицы
\node[ann, above=0.3em of ff] {2; 15--20; 210};
\node[ann, above=0.3em of tf] {3; 18--22; 4217};
\node[ann, above=0.3em of alph] {4; 2--5 и 33--45; 581014};

\end{tikzpicture}
}
\end{figure}
\end{document}
